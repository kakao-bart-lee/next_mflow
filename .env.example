# ──────────────────────────────────────────
# 데이터베이스
# ──────────────────────────────────────────
# Docker Compose 사용 시 (make db-up): 포트 5433
DATABASE_URL="postgresql://mflow:mflow_dev_pw@localhost:6532/next_mflow"
# 로컬 PostgreSQL 직접 사용 시:
# DATABASE_URL="postgresql://user:password@localhost:5432/next_mflow"

# ──────────────────────────────────────────
# NextAuth
# ──────────────────────────────────────────
# openssl rand -base64 32
AUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:4830"

# Google OAuth (https://console.cloud.google.com)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Kakao OAuth (https://developers.kakao.com)
KAKAO_CLIENT_ID=""
KAKAO_CLIENT_SECRET=""

# 개발 환경: 인증 건너뛰기 (true로 설정하면 dev 계정 자동 로그인)
SKIP_AUTH=false

# ──────────────────────────────────────────
# AI / LLM
# ──────────────────────────────────────────
OPENAI_API_KEY="sk-..."
# 사주 에이전트에 사용할 모델 (기본: gpt-4o-mini)
MASTRA_SAJU_MODEL="gpt-4o-mini"
# 점성 채팅 에이전트 모델 (미설정 시 MASTRA_SAJU_MODEL 사용)
MASTRA_ASTROLOGY_MODEL="gpt-4o-mini"
# true → 모든 LLM 호출 차단, Mock 응답 사용 (개발용)
MOCK_LLM=false
# Mastra Memory DB URL (비우면 file:./mastra-memory.db)
MASTRA_MEMORY_DB_URL=

# Haruna Horizons (외부 천체 계산 서비스)
# 비워두면 로컬 정적 계산 폴백 사용
HARUNA_HORIZONS_BASE_URL=""
HARUNA_HORIZONS_API_KEY=""
HARUNA_HORIZONS_TIMEOUT_MS=5000
# false로 설정하면 외부 서비스 호출 비활성화
ASTROLOGY_USE_HORIZONS=true

# ──────────────────────────────────────────
# 기능 플래그
# ──────────────────────────────────────────
# 크레딧 시스템 활성화 (false이면 무제한)
ENABLE_CREDIT_SYSTEM=false
# 구독 시스템 활성화
ENABLE_SUBSCRIPTION_SYSTEM=false
# 신규 가입 무료 크레딧
INITIAL_FREE_CREDITS=10
